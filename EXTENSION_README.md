# Pegasi 課堂安裝

課堂側邊欄：檢查／安裝環境工具（uv／git／Node），並一鍵執行本課 `classroom-installs.yaml` 中的安裝動作。

## 安裝（VS Code）

1. 在 VS Code 擴充功能市集搜尋「Pegasi 課堂安裝」，或依 id：`vans-coding.pegasi-classroom-install`。
2. 安裝後重新載入視窗（若提示）。
3. 於活動列開啟「Pegasi 課堂安裝」。

市集安裝後，VS Code 可能自動更新到較新版本；開課前請確認版本符合預期。

## 使用

1. 側邊欄最上方「課堂連線」：先輸入老師給的邀請碼 →「連線登入」→ 瀏覽器完成後回到編輯器即自動兌換（若沒自動回來，貼上一次性貼碼再按「貼上並完成連線」；可「重新連線登入」）。完成後按右下角「重新啟動」（完整重啟 VS Code，勿只按重載視窗），再開啟後選課堂已設定的模型（清單裡的 VCRouter）。Pegasi Router 指課堂後端網址，不是模型清單上的名字。
2. 把 `classroom-installs.yaml` 放到**工作區根目錄**（老師提供，或見專案示例）。
3. 「環境工具」（uv／git／Node）：未就緒就「安裝」，裝完依提示**重開終端**再「重新檢查」。
4. 「本課安裝」列出本課動作；點選 → 確認完整 command → 在整合終端機執行。

Portal 網頁兌換／下載腳本僅為備援。更完整的老師說明見專案倉庫 README。
